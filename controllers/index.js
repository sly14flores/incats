var app = angular.module('index', ['block-ui','bootstrap-notify','bootstrap-modal','account-module','notifications-module']);

app.controller('indexCtrl',function($scope,blockUI,bootstrapNotify,bootstrapModal) {

$scope.views = {};
	
});
